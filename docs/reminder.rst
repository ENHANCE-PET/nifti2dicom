Validation and limitations
==========================

Tests verify decoded values, independently calculated physical coordinates,
timepoint identity, SEG source-frame references and failure recovery.

Passing those tests or opening a file with pydicom is not a blanket DICOM
conformance or clinical validation claim. External IOD validation and tests
in the intended receiving viewer remain release acceptance requirements.
